---
- name: Update all servers
  hosts: allservers
  become: yes

  tasks:
    - name: Install EPEL repository (required for extra packages)
      ansible.builtin.dnf:
        name: epel-release
        state: present

    - name: update all packages
      ansible.builtin.dnf:
        name: "*"
        state: latest
